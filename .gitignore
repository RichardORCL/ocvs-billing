# --- Generated reports (getbilling.py CSV exports) ---
esxi_host_billing_*.csv
esxi_donor_hosts_*.csv

# --- Sensitive / credentials (never commit) ---
.env
.env.*
!.env.example
*.pem
*.key
*_private_key*
*api_key*.pem
# Local OCI config copy at repo root only (not subpaths named config)
/config
# OCI config often lives under ~/.oci; if copied into the repo:
.oci/

# --- Logs ---
*.log

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
.Python
venv/
.venv/
env/

# --- OS / editor ---
.DS_Store
Thumbs.db
*.swp
*~
